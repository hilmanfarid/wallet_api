# frozen_string_literal: true

RSpec.describe GemStyle do
  it "has a version number" do
    expect(GemStyle::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
